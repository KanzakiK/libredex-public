# LibreDeX 更新记录

## 0.1.8（2026-08-16）

### 修复

- AirPlay 启动后按 Moonlight 相同的时序（1.5s / 4s）把 APP 拉回手机屏幕前台，避免 DeX 桌面抢占前台后应用停留在后台。
- 可选投屏方式 tab 与 Moonlight、DP 使用同一按钮样式，高度、字号、大小写保持一致。

## 0.1.6（2026-08-16）

### 修复

- 可选投屏方式 tab 与 Moonlight、DP 使用同一按钮样式，高度、字号、大小写保持一致。

## 0.1.5（2026-08-16）

### 新增

- 单输出互斥：Moonlight、DP/HDMI 同一时间只保留一个输出；其他方式运行中时，对应页面会显示“停止服务”或不可选择，避免多个画面源同时输出。
- 日志导出改为 ZIP 压缩包：一次导出自动附带设备信息和运行环境摘要，分享更方便；下载目录会自动保留最近 5 份。
- 设置页新增后台服务（UserService）状态显示和一键重启入口，遇到服务异常时可以自己恢复。

### 修复

- 修复停止 DP/HDMI 输出后，外接屏不切回手机画面、画面冻结在最后一帧或直接变黑的问题；现在停止后会回到实时手机镜像。
- 修复 DP/HDMI 与 DeX/纯镜像之间切换时画面叠加、镜像层变黑的问题。
- 修复停止输出时外接屏反复闪烁、像在不断重试的问题。
- 修复输出结束后手机分辨率恢复时，主界面闪烁、被重新打开的问题。
- 修复日志无法导出、“无法获取日志目录”的问题。
- 修复 Sunshine 服务停止时状态卡住、按钮无法操作的问题。

### 调整

- 移除开机自启选项，安装/重启手机后不再自动启动 Sunshine 服务。

## 0.1.2（2026-08-15）

### 新增

- 输出链路统一抽象：`DEX / 纯镜像 × Moonlight / DP` 输出通道共用同一份画面源、镜像变换与会话生命周期逻辑；运行时按当前手机屏（内屏 / Flip 5 cover）适配。
- 纯镜像输出统一应用自动旋转、自动缩放去黑边、自动匹配宽高比。
- DP DeX 输出支持应用自由窗口（freeform），并按“DeX / 纯镜像”源选择正确启动对应会话。
- HEVC 编码器初始化失败时自动回退到 H.264/AVC，仅影响当前会话，下次服务启动重新尝试 HEVC。
- Sunshine 服务增加重复启动与初始化竞争守卫，降低“无法配置编码器”概率。
- fake screen 会话标记跨 UserService 重连保持，避免会话中按电源键退化为系统锁屏。

### 修复

- 修复纯镜像 + DP 未应用自动旋转、自动缩放去黑边、自动匹配宽高比的问题。
- 修复 DP 输出忽略画面源选择、纯镜像变成 DeX 的问题。
- 修复 DP 停止后外接屏 layerStack/投影未恢复的问题。
- 修复重复启动 Sunshine 导致编码器冲突的问题。
- 修复 UserService 重连把 `session_active` 清零导致假熄屏失效的问题。

### 其他

- 公开仓库发布流程整理。
