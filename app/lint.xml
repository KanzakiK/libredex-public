<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- i18n gate: hardcoded UI strings must go through resources.
         Phase 1 cleared layouts (HardcodedText); Phase 2 cleared Java
         setText/Toast/notify literals (SetTextI18n). Both are errors now. -->
    <issue id="HardcodedText" severity="error" />
    <issue id="SetTextI18n" severity="error" />

    <!-- Intentional hook-project tech debt: reflective hidden API access,
         root-scope permissions. Not i18n related; do not block builds. -->
    <issue id="NewApi" severity="warning" />
    <issue id="ProtectedPermissions" severity="warning" />
    <issue id="QueryAllPackagesPermission" severity="warning" />

    <!-- zh-rCN translations may lag behind values/; never fail for that. -->
    <issue id="MissingTranslation" severity="ignore" />
</lint>
